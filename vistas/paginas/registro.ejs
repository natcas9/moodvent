<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Registro</title>
    <link rel="stylesheet" href="/css/registro.css" />
  </head>
  <body>
    <div class="registro-container">
      <div class="left-panel">
        <!-- Aquí puedes poner imágenes o ilustraciones -->
      </div>

      <div class="right-panel">
        <h1>¡Bienvenido a Moodvent!</h1>
        <h2>Cuéntanos un poco sobre ti</h2>
        
        <form action="/usuarios/registro" method="POST">
          <div>
            <label for="nombre">Nombre:</label>
            <input type="text" id="nombre" name="nombre" value="<%= datos.nombre || '' %>" required />
            <% if (errores.nombre) { %>
              <span style="color:red"><%= errores.nombre.msg %></span>
            <% } %>
          </div>

          <div>
            <label for="apellido">Apellido:</label>
            <input type="text" id="apellido" name="apellido" value="<%= datos.apellido || '' %>" required />
            <% if (errores.apellido) { %>
              <span style="color:red"><%= errores.apellido.msg %></span>
            <% } %>
          </div>

          <div>
            <label for="edad">Edad:</label>
            <input type="number" id="edad" name="edad" value="<%= datos.edad || '' %>" required />
            <% if (errores.edad) { %>
              <span style="color:red"><%= errores.edad.msg %></span>
            <% } %>
          </div>

          <div>
            <label for="email">Correo:</label>
            <input type="email" id="email" name="email" value="<%= datos.email || '' %>" required />
            <% if (errores.email) { %>
              <span style="color:red"><%= errores.email.msg %></span>
            <% } %>
          </div>

          <div>
            <label for="username">Usuario:</label>
            <input type="text" id="username" name="username" value="<%= datos.username || '' %>" required />
            <% if (errores.username) { %>
              <span style="color:red"><%= errores.username.msg %></span>
            <% } %>
          </div>

          <div>
            <label for="password">Contraseña:</label>
            <input type="password" id="password" name="password" required />
            <% if (errores.password) { %>
              <span style="color:red"><%= errores.password.msg %></span>
            <% } %>
          </div>

          <div>
            <label for="role">Rol:</label>
            <select name="role" id="role" required>
              <option value="">Selecciona un rol</option>
              <option value="user" <%= datos.role === "user" ? "selected" : "" %>>Usuario</option>
              <option value="admin" <%= datos.role === "admin" ? "selected" : "" %>>Administrador</option>
            </select>
            <% if (errores.role) { %>
              <span style="color:red"><%= errores.role.msg %></span>
            <% } %>
          </div>

          <button type="submit">Registrarse</button>
        </form>

        <p class="login-link">
          ¿Ya tienes cuenta? <a href="/usuarios/login">Inicia sesión</a>
        </p>
      </div>
    </div>
  </body>
</html>
